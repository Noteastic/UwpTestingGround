<Page
    x:Class="Presentation.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer x:Name="ScrollViewer" ZoomMode="Enabled" ViewChanged="ScrollViewer_ViewChanged" MinZoomFactor="0.5">
            <Grid Height="100000">
                <local:LargeCanvasVirtualControl x:Name="CanvasVirtualControl"
                                                 HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                                 RenderCommand="{x:Bind RenderCommand, Mode=OneTime}"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
